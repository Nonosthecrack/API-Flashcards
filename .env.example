# Chemin vers le fichier SQLite (local) ou URL Turso (production)
DB_FILE=file:./flashcards.db

# Clé secrète pour signer les JWT — change cette valeur en production !
JWT_SECRET=change_me_in_production

# Port d'écoute du serveur
PORT=3000
